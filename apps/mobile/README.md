# @finance/mobile

Placeholder for the future React Native + Expo app (Phase 10).

Will share with the rest of the platform:

- `@finance/contracts` (API contracts and Zod schemas)
- `@finance/auth` (token abstractions)
- non-visual logic

Web components are NOT shared with React Native.
